// EXPANDED STATS CLEAN V2 - 2026-09-05
import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';

const sbLjs=createClient('https://jzrbaeyvwagrwukntjbk.supabase.co','sb_publishable_OVczf1AxPQfYdwynkVlwaQ_9fdB9ij8');
let ctx=null,journey=null,profiles=null,mode=null,primary=null,rival='',statsUser=null,timer=null;
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const ord=n=>{n=+n||0;const s=['th','st','nd','rd'],v=n%100;return`${n}${s[(v-20)%10]||s[v]||s[0]}`};
const active=v=>!!document.querySelector(`#nav button[data-v="${v}"].active`);

function css(){if(document.getElementById('ljs-v2-css'))return;const s=document.createElement('style');s.id='ljs-v2-css';s.textContent=`
.ljs2-card{background:rgba(255,255,255,.97)!important;border:1px solid #e7e4ef!important;border-radius:18px;padding:15px;margin-bottom:13px;box-shadow:0 8px 24px rgba(25,18,65,.055)}
.ljs2-head{display:flex;justify-content:space-between;gap:10px;align-items:flex-start;flex-wrap:wrap}.ljs2-head h2,.ljs2-title h3{margin:0}.ljs2-head h2{font-size:17px}.ljs2-title{display:flex;justify-content:space-between;gap:8px;align-items:flex-end;margin:14px 0 8px}.ljs2-title h3{font-size:14px}.ljs2-title small{font-size:8.5px;color:#858190;text-align:right}
.ljs2-toggle{display:flex;background:#f0eff5;border-radius:11px;padding:3px}.ljs2-toggle button{border:0;background:transparent;border-radius:9px;padding:7px 10px;font-size:10px;font-weight:900;color:#6f6b7d}.ljs2-toggle button.on{background:#fff;color:#4b269d;box-shadow:0 2px 8px rgba(30,20,70,.1)}
.ljs2-selects{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:11px 0}.ljs2-selects label,.ljs2-player label{display:block;font-size:9px;font-weight:950;color:#777487;text-transform:uppercase;margin:0 0 4px 2px}.ljs2-selects select,.ljs2-player select{width:100%;border:1px solid #ddd9e9;border-radius:11px;padding:9px 10px;background:#fff;color:#252137;font-size:11px;font-weight:800}
.ljs2-graph{overflow-x:auto;-webkit-overflow-scrolling:touch;border:1px solid #ebe8f2;border-radius:14px;background:#fff}.ljs2-graph svg{display:block}.ljs2-legend,.ljs2-pills{display:flex;gap:6px;flex-wrap:wrap;margin-top:9px}.ljs2-legend{font-size:9px;color:#625e70;font-weight:850}.ljs2-dot{display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:5px}.ljs2-dot.me{background:#5a35b1}.ljs2-dot.rival{background:#d17a00}.ljs2-dot.other{background:#c9c7d3}.ljs2-hint{font-size:9.5px;color:#817d8d;line-height:1.45;margin-top:8px}
.ljs2-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:7px;margin-top:10px}.ljs2-stat{background:#f6f5fa;border-radius:12px;padding:9px 5px;text-align:center;min-width:0}.ljs2-stat b{display:block;font-size:18px}.ljs2-stat span{font-size:8px;color:#797586;font-weight:900;text-transform:uppercase}.ljs2-pill{background:#f0edf9;color:#51398b;border-radius:999px;padding:5px 8px;font-size:9px;font-weight:850}.ljs2-pill.good{background:#e5f8f2;color:#08775c}.ljs2-pill.bad{background:#fdebed;color:#a52b3a}.ljs2-pill.neutral{background:#f1f0f5;color:#686474}
.ljs2-two{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:9px}.ljs2-box{border:1px solid #ebe8f2;border-radius:13px;padding:10px;background:#faf9fd}.ljs2-box h4{font-size:10px;margin:0 0 5px;color:#6b6575;text-transform:uppercase}.ljs2-box b{font-size:13px}.ljs2-box p{font-size:9px;color:#817d8d;margin:3px 0 0;line-height:1.35}
.ljs2-form{display:grid;grid-template-columns:repeat(5,1fr);gap:6px;margin-top:8px}.ljs2-pick{border-radius:11px;text-align:center;padding:8px 3px;font-weight:950;font-size:15px;background:#f0eff5;color:#625e70}.ljs2-pick.exact{background:#e5f8f2;color:#08775c}.ljs2-pick.one{background:#fff3cf;color:#7b5a00}.ljs2-pick.zero{background:#fdebed;color:#a52b3a}.ljs2-pick small{display:block;font-size:7px;margin-top:2px;opacity:.75}
.ljs2-row{display:flex;justify-content:space-between;gap:10px;padding:8px 0;border-bottom:1px solid #eeecf3;font-size:11px;align-items:center}.ljs2-row:last-child{border-bottom:0}.ljs2-row small{display:block;color:#858190;font-size:9px;margin-top:2px}.ljs2-bars{display:grid;gap:7px}.ljs2-bar{display:grid;grid-template-columns:82px 1fr auto;gap:7px;align-items:center;font-size:9px;font-weight:850;color:#5f5a6b}.ljs2-track{height:8px;background:#ebe9f1;border-radius:999px;overflow:hidden}.ljs2-track i{display:block;height:100%;background:#6a47ba;border-radius:999px}.ljs2-empty{padding:15px;text-align:center;color:#777487;font-size:11px}
@media(max-width:520px){.ljs2-selects{grid-template-columns:1fr}.ljs2-two{grid-template-columns:1fr 1fr}.ljs2-bar{grid-template-columns:74px 1fr auto}}
`;document.head.appendChild(s)}

async function context(){if(ctx)return ctx;try{const{data:{session}}=await sbLjs.auth.getSession();if(!session)return null;const{data,error}=await sbLjs.from('league_members').select('league_id,status,joined_at').eq('user_id',session.user.id).eq('status','active').order('joined_at',{ascending:false}).limit(1);if(error)throw error;if(!data?.length)return null;ctx={userId:session.user.id,leagueId:data[0].league_id};return ctx}catch(e){console.warn('Stats context',e);return null}}
async function loadJourney(force=false){const c=await context();if(!c)throw Error('League not ready');if(journey&&!force)return journey;const{data,error}=await sbLjs.rpc('get_league_journey',{p_league_id:c.leagueId});if(error)throw error;journey=data||{};mode=mode||journey.default_mode||'daily';primary=primary||journey.current_user_id||c.userId;return journey}
async function loadProfiles(force=false){const c=await context();if(!c)throw Error('League not ready');if(profiles&&!force)return profiles;const{data,error}=await sbLjs.rpc('get_player_stats_profiles',{p_league_id:c.leagueId});if(error)throw error;profiles=data||{};statsUser=statsUser||profiles.current_user_id||c.userId;return profiles}

const entrant=(d,id)=>(d.entrants||[]).find(x=>String(x.entrant_id)===String(id));
const pos=(cp,id)=>(cp?.positions||[]).find(x=>String(x.entrant_id)===String(id));
function graphSvg(d){const cps=mode==='matchweek'?(d.matchweeks||[]): (d.daily||[]),ens=d.entrants||[];if(!cps.length||!ens.length)return'<div class="ljs2-empty">The journey graph will build as results are scored.</div>';const n=ens.length,compact=cps.length<=5,w=compact?340:Math.max(560,54+(cps.length-1)*62),h=314,L=34,R=18,T=22,B=37,ph=h-T-B,x=i=>cps.length===1?L+(w-L-R)/2:L+i*((w-L-R)/(cps.length-1)),y=p=>T+((+p-1)/Math.max(1,n-1))*ph,marks=[...new Set([1,5,10,15,20,25,n].filter(v=>v<=n))];let out=`<svg ${compact?'width="100%"':`width="${w}"`} height="${h}" viewBox="0 0 ${w} ${h}" xmlns="http://www.w3.org/2000/svg">`;marks.forEach(m=>{const yy=y(m);out+=`<line x1="${L}" y1="${yy}" x2="${w-R}" y2="${yy}" stroke="#ece9f2"/><text x="${L-6}" y="${yy+3}" text-anchor="end" font-size="8.5" fill="#8a8695">${m===1?'1st':m}</text>`});cps.forEach((cp,i)=>out+=`<text x="${x(i)}" y="${h-12}" text-anchor="middle" font-size="8.5" font-weight="700" fill="#7d7889">${esc(cp.label||'')}</text>`);ens.slice().sort((a,b)=>{const A=String(a.entrant_id)===String(primary)?2:String(a.entrant_id)===String(rival)?1:0,Bb=String(b.entrant_id)===String(primary)?2:String(b.entrant_id)===String(rival)?1:0;return A-Bb}).forEach(e=>{const pts=[];cps.forEach((cp,i)=>{const p=pos(cp,e.entrant_id);if(p)pts.push(`${x(i)},${y(p.position)}`)});if(!pts.length)return;const me=String(e.entrant_id)===String(primary),rv=String(e.entrant_id)===String(rival),stroke=me?'#5a35b1':rv?'#d17a00':'#c9c7d3',sw=me||rv?3.8:1.25,op=me||rv?1:.34;out+=`<polyline points="${pts.join(' ')}" fill="none" stroke="${stroke}" stroke-width="${sw}" opacity="${op}" stroke-linecap="round" stroke-linejoin="round"${e.entrant_type==='ai'?' stroke-dasharray="5 4"':''}/>`;if(me||rv)cps.forEach((cp,i)=>{const p=pos(cp,e.entrant_id);if(p)out+=`<circle cx="${x(i)}" cy="${y(p.position)}" r="3.8" fill="${stroke}" stroke="#fff" stroke-width="1.3"><title>${esc(e.team_name)} · ${esc(cp.label)} · ${ord(p.position)} · ${p.points} pts</title></circle>`})});return out+'</svg>'}
function journeyHtml(d){const ens=d.entrants||[],cps=mode==='matchweek'?(d.matchweeks||[]): (d.daily||[]),last=cps[cps.length-1],first=cps[0],me=entrant(d,primary),mp=pos(last,primary),fp=pos(first,primary),rp=entrant(d,rival),rpp=pos(last,rival);const options=ens.map(e=>`<option value="${esc(e.entrant_id)}" ${String(e.entrant_id)===String(primary)?'selected':''}>${esc(e.badge||'⚽')} ${esc(e.team_name)}${String(e.entrant_id)===String(d.current_user_id)?' (You)':''}</option>`).join(''),rOpts=ens.filter(e=>String(e.entrant_id)!==String(primary)).map(e=>`<option value="${esc(e.entrant_id)}" ${String(e.entrant_id)===String(rival)?'selected':''}>${esc(e.badge||'⚽')} ${esc(e.team_name)}</option>`).join(''),move=fp&&mp?fp.position-mp.position:0;return`<div class="ljs2-head"><div><h2>📈 League Journey</h2><div class="notice">See how every team has moved through the league.</div></div><div class="ljs2-toggle"><button data-mode="daily" class="${mode==='daily'?'on':''}">Detailed</button><button data-mode="matchweek" class="${mode==='matchweek'?'on':''}">Matchweek</button></div></div><div class="ljs2-selects"><div><label>Highlight</label><select data-primary>${options}</select></div><div><label>Compare with</label><select data-rival><option value="">No rival selected</option>${rOpts}</select></div></div><div class="ljs2-graph">${graphSvg(d)}</div>${mp?`<div class="ljs2-pills"><span class="ljs2-pill neutral">Now ${ord(mp.position)}</span>${fp?`<span class="ljs2-pill neutral">Started ${ord(fp.position)}</span>`:''}<span class="ljs2-pill ${move>0?'good':move<0?'bad':'neutral'}">${move>0?'↑ '+move:move<0?'↓ '+Math.abs(move):'No net move'} place${Math.abs(move)===1?'':'s'}</span></div>`:''}<div class="ljs2-legend"><span><i class="ljs2-dot me"></i>${esc(me?.team_name||'Selected')}${mp?` · ${ord(mp.position)}`:''}</span>${rp?`<span><i class="ljs2-dot rival"></i>${esc(rp.team_name)}${rpp?` · ${ord(rpp.position)}`:''}</span>`:''}<span><i class="ljs2-dot other"></i>Rest of league</span></div><div class="ljs2-hint">${mode==='daily'?'Detailed updates after each scoring day.':'Matchweek updates only after a full fixture round is complete.'}${Number(d.latest_closed_matchweek||0)<6?' Detailed remains the default until MW6 is complete.':''} AI is dashed.</div>`}
function bindJourney(card,d){card.querySelectorAll('[data-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.mode;card.innerHTML=journeyHtml(d);bindJourney(card,d)});card.querySelector('[data-primary]')?.addEventListener('change',e=>{primary=e.target.value;if(String(rival)===String(primary))rival='';card.innerHTML=journeyHtml(d);bindJourney(card,d)});card.querySelector('[data-rival]')?.addEventListener('change',e=>{rival=e.target.value;card.innerHTML=journeyHtml(d);bindJourney(card,d)})}

function mv(v){v=+v||0;return v>0?`<b style="color:#08775c">↑ ${v}</b>`:v<0?`<b style="color:#b52c3c">↓ ${Math.abs(v)}</b>`:'<b>—</b>'}
function profileHtml(d){const ps=d.players||[];let p=ps.find(x=>String(x.user_id)===String(statsUser))||ps[0];if(!p)return'<div class="ljs2-empty">Player stats will appear as results are scored.</div>';statsUser=p.user_id;const opts=ps.map(x=>`<option value="${x.user_id}" ${String(x.user_id)===String(p.user_id)?'selected':''}>${esc(x.badge||'⚽')} ${esc(x.team_name||x.display_name)}${String(x.user_id)===String(d.current_user_id)?' (You)':''}</option>`).join(''),r=p.recent_picks||[],calls=p.call_accuracy||{},st=p.prediction_style||{},pop=p.most_predicted_score,best=p.best_matchweek,worst=p.worst_matchweek,mws=p.matchweeks||[],la=p.league_average||{},bestClub=p.best_club,nem=p.nemesis_club,vs=+p.points_vs_league_average||0,ev=+p.exacts_vs_league_average||0,total=Math.max(1,+p.points||0),exactShare=Math.round((+p.exact_points||0)*100/total),outShare=Math.round((+p.outcome_points||0)*100/total),sum=(+st.home||0)+(+st.draw||0)+(+st.away||0),pct=n=>sum?Math.round((+n||0)*100/sum):0,goalBias=st.avg_goals!=null&&st.actual_avg_goals!=null?Number(st.avg_goals)-Number(st.actual_avg_goals):null,call=(name,key)=>{const c=calls[key]||{};return`<div class="ljs2-box"><h4>${name} calls</h4><b>${c.scoring_pct??0}% score</b><p>${c.scored??0} scoring picks from ${c.calls??0}</p></div>`},form=r.length?r.map(x=>`<div class="ljs2-pick ${+x.points===3?'exact':+x.points===1?'one':'zero'}">${x.points}<small>${+x.points===3?'EXACT':+x.points===1?'1 PT':'0'}</small></div>`).join(''):'<div class="notice">Form will build as fixtures are scored.</div>';return`
<div class="ljs2-card ljs2-player"><div class="ljs2-head"><div><h2>👤 Player Stats</h2><div class="notice">Switch player to explore anyone in the league.</div></div></div><label style="margin-top:10px">Viewing</label><select data-stats-user>${opts}</select><div class="ljs2-grid"><div class="ljs2-stat"><b>${ord(p.current_position)}</b><span>Live position</span></div><div class="ljs2-stat"><b>${p.points??0}</b><span>Points</span></div><div class="ljs2-stat"><b>${p.exacts??0}</b><span>Exacts</span></div><div class="ljs2-stat"><b>${p.scoring_pct??0}%</b><span>Scoring picks</span></div><div class="ljs2-stat"><b>${p.points_per_prediction??0}</b><span>Pts / pick</span></div><div class="ljs2-stat"><b>${p.missed??0}</b><span>Missed</span></div></div><div class="ljs2-title"><h3>📍 Against the league</h3><small>Live totals</small></div><div class="ljs2-pills"><span class="ljs2-pill good">Ahead of ${p.beating_percent??0}%</span><span class="ljs2-pill ${vs>=0?'good':'bad'}">${vs>=0?'+':''}${vs} pts vs avg</span><span class="ljs2-pill ${ev>=0?'good':'bad'}">${ev>=0?'+':''}${ev} exacts vs avg</span><span class="ljs2-pill neutral">${p.gap_to_above??0} pts to place above</span><span class="ljs2-pill neutral">${p.gap_to_leader??0} pts off leader</span></div><div class="ljs2-two"><div class="ljs2-box"><h4>Season high</h4><b>${ord(p.highest_position)}</b><p>Best live league position</p></div><div class="ljs2-box"><h4>Season low</h4><b>${ord(p.lowest_position)}</b><p>Lowest live league position</p></div></div></div>
<div class="ljs2-card"><div class="ljs2-title" style="margin-top:0"><h3>🔥 Current form</h3><small>Latest five scored fixtures</small></div><div class="ljs2-form">${form}</div><div class="ljs2-grid"><div class="ljs2-stat"><b>${p.last5_points??0}</b><span>Last 5 pts</span></div><div class="ljs2-stat"><b>${p.last5_scoring??0}/5</b><span>Scored</span></div><div class="ljs2-stat"><b>${p.last5_exacts??0}</b><span>Last 5 exacts</span></div><div class="ljs2-stat"><b>${p.current_scoring_streak??0}</b><span>Current streak</span></div><div class="ljs2-stat"><b>${p.longest_scoring_streak??0}</b><span>Best streak</span></div><div class="ljs2-stat"><b>${p.longest_exact_streak??0}</b><span>Exact streak</span></div></div></div>
<div class="ljs2-card"><div class="ljs2-title" style="margin-top:0"><h3>🏆 Matchweek performance</h3><small>${d.closed_matchweeks??0} complete</small></div><div class="ljs2-grid"><div class="ljs2-stat"><b>${p.matchweek_wins??0}</b><span>MW wins</span></div><div class="ljs2-stat"><b>${p.top_three_finishes??0}</b><span>Top 3s</span></div><div class="ljs2-stat"><b>${p.weeks_above_average??0}</b><span>Above avg</span></div></div><div class="ljs2-pills"><span class="ljs2-pill">Last completed MW ${mv(p.last_mw_movement)}</span><span class="ljs2-pill">${p.avg_points_per_matchweek??0} avg pts / MW</span></div>${best?`<div class="ljs2-row"><span><b>Best round</b><small>MW${best.matchweek}</small></span><b>${best.points} pts · ${ord(best.position)}</b></div>`:''}${worst?`<div class="ljs2-row"><span><b>Lowest round</b><small>MW${worst.matchweek}</small></span><b>${worst.points} pts · ${ord(worst.position)}</b></div>`:''}${mws.length?`<div class="ljs2-title"><h3>📅 Matchweek history</h3><small>Completed rounds</small></div>${mws.map(x=>`<div class="ljs2-row"><span><b>MW${x.matchweek}</b><small>${x.exacts} exact${+x.exacts===1?'':'s'} · weekly ${ord(x.weekly_position)}</small></span><span style="text-align:right"><b>${x.points} pts</b><small>League ${ord(x.league_position)}</small></span></div>`).join('')}`:''}</div>
<div class="ljs2-card"><div class="ljs2-title" style="margin-top:0"><h3>🎯 How the points were won</h3><small>${p.predictions_scored??0} predictions scored</small></div><div class="ljs2-bars"><div class="ljs2-bar"><span>Exact scores</span><div class="ljs2-track"><i style="width:${Math.min(100,exactShare)}%"></i></div><b>${p.exact_points??0} pts</b></div><div class="ljs2-bar"><span>Outcomes</span><div class="ljs2-track"><i style="width:${Math.min(100,outShare)}%"></i></div><b>${p.outcome_points??0} pts</b></div><div class="ljs2-bar"><span>Scoring picks</span><div class="ljs2-track"><i style="width:${Math.min(100,+p.scoring_pct||0)}%"></i></div><b>${p.scoring_picks??0}/${p.predictions_scored??0}</b></div></div><div class="ljs2-title"><h3>🧠 Call accuracy</h3><small>By predicted outcome</small></div><div class="ljs2-two">${call('Home','home')}${call('Draw','draw')}${call('Away','away')}<div class="ljs2-box"><h4>Exact rate</h4><b>${p.exact_pct??0}%</b><p>${p.exacts??0} exact scores</p></div></div><div class="ljs2-title"><h3>⚽ Club reads</h3><small>Early sample — improves with time</small></div><div class="ljs2-two"><div class="ljs2-box"><h4>Best club to call</h4>${bestClub?`<b>${esc(bestClub.club)}</b><p>${bestClub.points} pts from ${bestClub.matches} fixtures · ${bestClub.scoring_pct}% scored</p>`:'<b>Building…</b>'}</div><div class="ljs2-box"><h4>Nemesis club</h4>${nem?`<b>${esc(nem.club)}</b><p>${nem.points} pts from ${nem.matches} fixtures · ${nem.scoring_pct}% scored</p>`:'<b>Building…</b>'}</div></div><div class="ljs2-title"><h3>🔮 Prediction DNA</h3><small>How they tend to call games</small></div><div class="ljs2-pills"><span class="ljs2-pill">Home ${pct(st.home)}%</span><span class="ljs2-pill">Draw ${pct(st.draw)}%</span><span class="ljs2-pill">Away ${pct(st.away)}%</span>${pop?`<span class="ljs2-pill">Favourite ${pop.home}–${pop.away} ×${pop.uses}</span>`:''}${st.avg_goals!=null?`<span class="ljs2-pill">${st.avg_goals} predicted goals/match</span>`:''}${st.actual_avg_goals!=null?`<span class="ljs2-pill">${st.actual_avg_goals} actual goals/match</span>`:''}${goalBias!=null?`<span class="ljs2-pill neutral">${goalBias>0?'+':''}${goalBias.toFixed(2)} goal bias</span>`:''}</div></div>`}
function bindProfile(container,d){container.querySelector('[data-stats-user]')?.addEventListener('change',e=>{statsUser=e.target.value;renderStats(container,d)})}
function renderStats(container,d){container.innerHTML=profileHtml(d);bindProfile(container,d)}

async function injectJourney(){if(!active('table'))return;const main=document.getElementById('main');if(!main||main.querySelector('.ljs2-journey'))return;const h=[...main.querySelectorAll('.card .section h2')].find(x=>x.textContent.trim()==='League table');if(!h)return;const card=document.createElement('div');card.className='ljs2-card ljs2-journey';card.innerHTML='<div class="ljs2-empty">Building League Journey…</div>';h.closest('.card').insertAdjacentElement('afterend',card);try{const d=await loadJourney();if(card.isConnected){card.innerHTML=journeyHtml(d);bindJourney(card,d)}}catch(e){if(card.isConnected)card.innerHTML=`<div class="ljs2-empty">Couldn’t load journey yet.<br>${esc(e.message)}</div>`}}
async function injectStats(){if(!active('stats'))return;const main=document.getElementById('main');if(!main||main.querySelector('.ljs2-playerHost'))return;const h=[...main.querySelectorAll('.card .section h2')].find(x=>x.textContent.replace('📊','').trim()==='Stats');if(!h)return;const host=document.createElement('div');host.className='ljs2-playerHost';host.innerHTML='<div class="ljs2-card"><div class="ljs2-empty">Building player stats…</div></div>';h.closest('.card').insertAdjacentElement('afterend',host);try{const d=await loadProfiles();const old=[...main.querySelectorAll('.card .section h2')].find(x=>x.textContent.trim()==='Your season')?.closest('.card');if(old)old.style.display='none';if(host.isConnected)renderStats(host,d)}catch(e){if(host.isConnected)host.innerHTML=`<div class="ljs2-card"><div class="ljs2-empty">Couldn’t load player stats yet.<br>${esc(e.message)}</div></div>`}}
async function inject(){css();await injectJourney();await injectStats()}
setTimeout(inject,700);new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(inject,180)}).observe(document.body,{childList:true,subtree:true});setInterval(inject,1800);
